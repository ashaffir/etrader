"""Unit tests for the etrader bot.

These tests use only the Python standard library so they can run in any
environment that has Python 3.11+. Modules that import third-party
dependencies (``requests``, ``openai``) are not exercised here; their
behavior is tested via dependency-injected fakes elsewhere.
"""
